/**
 * UI Components - Các component UI dùng chung trong toàn bộ ứng dụng
 */

import PageHeader from "./PageHeader";
import NoDataMessage from "./NoDataMessage";
import DashboardCard from "./DashboardCard";

// Export all UI components
export { PageHeader, NoDataMessage, DashboardCard };

// Default export
export default {
  PageHeader,
  NoDataMessage,
  DashboardCard,
};

// Export các component khác nếu có
